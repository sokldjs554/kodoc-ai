# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# 환경/시크릿
.env

# 인덱스/산출물
index/
*.npy
# LoRA 어댑터 가중치 — 바이너리라 커밋하지 않는다. 재현에 필요한 것은
# 학습 데이터(finetune/data/)와 평가 결과(finetune/results/)이고 그쪽은 커밋한다.
finetune/out/
# benchmarks/results/ 는 커밋한다 — 측정 환경(GPU/드라이버/엔진 버전)이 JSON에
# 함께 기록되므로 결과 자체가 재현 가능한 실험 기록이 된다.
.pytest_cache/
.ruff_cache/

# OS/에디터
.DS_Store
.idea/
.vscode/
